import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#006878',
		},
		secondary: {
			main: '#040505',
		},
		background: {
			main: '#040505',
		},
	},
});

export default theme;
